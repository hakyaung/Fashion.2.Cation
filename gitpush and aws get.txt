# 1. 변경된 모든 파일을 장바구니에 담기
git add .

# 2. 이번 업데이트가 무엇인지 이름표 붙이기 (포장)
git commit -m "🌐 API 주소를 도메인(fashion2cation.co.kr)으로 최종 변경"

# 3. GitHub로 발사!
git push origin main


# 1. 프로젝트 폴더로 이동 (이미 폴더 안이라면 생략 가능)
cd ~/Fashion.2.Cation

# 2. GitHub에 올라온 따끈따끈한 코드 가져오기
git pull origin main


- 리액트
1. 로컬(내 컴퓨터)에서 코드 밀어넣기
git add .
git commit -m "Fix: mobile upload error and token sanitization"
git push origin main  # (혹은 사용하시는 브랜치명)

2. AWS 서버에서 코드 당겨오기
cd ~/Fashion.2.Cation
git pull origin main

3. 프론트엔드 다시 굽기 (가장 중요! ⭐)
cd frontend_react
npm install 		 # (혹시 모르니 패키지 체크)
npm run build

4. 백엔드 재시작 (깔끔하게)
cd ..
sudo fuser -k 8000/tcp
nohup python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 &